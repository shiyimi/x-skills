# Project Overview

> 本文件描述当前项目的业务上下文、模块边界和架构关系。它是 AI 理解项目的叙述型入口。

## 业务背景

`<说明项目解决什么问题、服务哪些用户、在业务链路中的位置。>`

## 应用与模块边界

| 应用 / 模块 | 职责 | 不负责 | 主要依赖 |
| --- | --- | --- | --- |
| `<MODULE_NAME>` | `<RESPONSIBILITY>` | `<OUT_OF_SCOPE>` | `<DEPENDENCIES>` |

## 核心数据流

`<描述用户操作、页面或服务、状态、接口、存储、外部系统之间的主要流转。>`

## 核心领域概念

| 概念 | 含义 | 关键约束 |
| --- | --- | --- |
| `<DOMAIN_TERM>` | `<MEANING>` | `<CONSTRAINTS>` |

## 关键状态与流程

`<描述关键状态机、审批流、任务流、生命周期或不可乱序的业务过程。>`

## 架构约束

- `<约束 1>`
- `<约束 2>`
- `<约束 3>`

## 高频变更区域

- `<HOTSPOT_1>`：`<为什么经常改，改动时要注意什么>`
- `<HOTSPOT_2>`：`<为什么经常改，改动时要注意什么>`

## 非目标

- `<本项目明确不负责的事情>`
- `<容易被误认为属于本项目但实际不属于的事情>`
